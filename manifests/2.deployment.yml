apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  labels:
    app: nginx
spec:
  replicas: 4
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: mysqldb
        image: sravanjyothi/clientapp:appimage
        ports:
        - containerPort: 80
        env:
            - name: DB_HOST
              value: mysqldb
      imagePullSecrets:
            - name: mysecret
